{
  "name": "emessenger",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @emessenger/shared && concurrently -n backend,admin,web -c green,blue,magenta \"npm run dev -w @emessenger/backend\" \"npm run dev -w @emessenger/admin\" \"npm run dev -w @emessenger/web\"",
    "build": "npm run build -w @emessenger/shared && npm run build -w @emessenger/backend && npm run build -w @emessenger/admin && npm run build -w @emessenger/web",
    "db:generate": "npm run db:generate -w @emessenger/backend",
    "db:migrate": "npm run db:migrate -w @emessenger/backend",
    "db:seed": "npm run db:seed -w @emessenger/backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
